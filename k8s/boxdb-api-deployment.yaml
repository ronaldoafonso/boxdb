---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: boxdb-api
  namespace: boxdb
spec:
  selector:
    matchLabels:
      app: boxdb
      version: v1.0
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 3
  template:
    metadata:
      labels:
        app: boxdb
        version: v1.0
    spec:
      containers:
        - name: boxdb
          image: ronaldoafonso/boxdb:0.0.4
          ports:
            - containerPort: 5000
              name: boxdb
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 60
